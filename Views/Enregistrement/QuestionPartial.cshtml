@model IEnumerable<Forum_descussion_ASP.NET_core_mvc.Models.QuestionModel>

<div>
   
    @foreach (var item in Model) {
        <div>
            <strong> @Html.DisplayFor(modelItem => item.Titre) </strong>
        </div>
     
    }
         
</div>


